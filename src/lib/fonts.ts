import { Pathway_Extreme, Oswald } from 'next/font/google';

export const pathwayExtreme = Pathway_Extreme({
  variable: '--font-pathway-extreme',
  subsets: ['latin'],
});

export const oswald = Oswald({
  variable: '--font-oswald',
  subsets: ['latin'],
});
